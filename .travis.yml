language: generic 
sudo: required
services:
  - docker

before_install:
  - docker build -t catarinafranco/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true catarinafranco/docker-react npm run test